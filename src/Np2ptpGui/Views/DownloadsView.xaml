<UserControl x:Class="Np2ptpGui.Views.DownloadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Np2ptpGui.Controls">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="500" Text="{Binding DownloadLinkInput, UpdateSourceTrigger=PropertyChanged}"
                     controls:HintBehavior.Hint="Cole aqui um link np2ptp:... ou caminho de um .nptp" />
            <Button Content="+ New download" Command="{Binding StartDownloadCommand}" Margin="10,0,0,0" />
        </StackPanel>
        <ListView ItemsSource="{Binding FetchOperations}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Link" DisplayMemberBinding="{Binding InputOrLink}" Width="220" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="80" />
                    <GridViewColumn Header="Detail" DisplayMemberBinding="{Binding DetailText}" Width="180" />
                    <GridViewColumn Header="Progress" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Minimum="0" Maximum="1" Value="{Binding ProgressFraction, Mode=OneWay}" Width="100" Height="16" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Stop" Padding="8,4"
                                            Command="{Binding DataContext.StopOperationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding Id}" />
                                    <Button Content="Retry" Padding="8,4" Margin="4,0,0,0"
                                            Command="{Binding DataContext.RetryOperationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding Id}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
