<UserControl x:Class="Np2ptpGui.Views.ShareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Np2ptpGui.Controls">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="380" Text="{Binding PackInputPath, UpdateSourceTrigger=PropertyChanged}"
                     controls:HintBehavior.Hint="Arquivo ou pasta a compartilhar" />
            <Button Content="Arquivo..." Command="{Binding BrowsePackFileCommand}" Margin="10,0,0,0" />
            <Button Content="Pasta..." Command="{Binding BrowsePackFolderCommand}" Margin="5,0,0,0" />
            <Button Content="+ Pack" Command="{Binding StartPackCommand}" Margin="10,0,0,0" />
        </StackPanel>
        <ListView ItemsSource="{Binding PackOperations}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Input" DisplayMemberBinding="{Binding InputOrLink}" Width="260" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="80" />
                    <GridViewColumn Header="Detail" DisplayMemberBinding="{Binding DetailText}" Width="220" />
                    <GridViewColumn Header="" Width="210">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Copy" Padding="8,4"
                                            Command="{Binding DataContext.CopyLinkCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding Id}" />
                                    <Button Content="Retry" Padding="8,4" Margin="4,0,0,0"
                                            Command="{Binding DataContext.RetryOperationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding Id}" />
                                    <Button Content="Delete" Padding="8,4" Margin="4,0,0,0"
                                            Command="{Binding DataContext.DeleteOperationCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding Id}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
